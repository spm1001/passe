The usability audit (1,199 invocations on Mac, 108 on kube) is the ground truth for grammar decisions. Naive survey (n=43) shows what Claudes discover; trained audit shows what they actually use. The gap between "taught" and "used" is the kill signal: wait-for (0 uses), wait-idle (0 uses), click-text (0 uses), click-if (0 uses), wait-navigation (0 uses). snapshot (25 uses) is the one surprise survivor — scout-then-act works when taught. Always check ~/Taildrive/passe-usability.txt before starting grammar work. The audit script itself had a bias bug — it flagged seconds-based waits as errors, counting correct instincts as mistakes.