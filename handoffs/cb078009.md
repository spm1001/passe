# Handoff — 2026-03-31

session_id: cb078009-b8b1-4f5f-85ad-b90baf32bca6
purpose: Fixed Chrome Passe popup debugger-pause bug caused by NSWorkspace launch

## Done
- Fixed Chrome Passe launch method: NSWorkspace.openApplication() → Process() + open -a
- Root-caused "Debugger paused in another tab" bug breaking Meet screenshare
- Deduplicated chrome-passe.md (docs/ is canonical, root copy removed)
- Added --disable-infobars to Chrome Passe launch flags
- Explored computer-use MCP: browsers read-only, native apps get full interaction
- Filed passe-cinuje for untested openURLs NSWorkspace path

## Gotchas
- The bug ONLY reproduces with real physical user clicks — CDP-dispatched clicks don't trigger it. Any future debugging must account for this observer effect.
- `--disable-extensions` masks the bug but isn't the fix. Extensions + NSWorkspace launch was the combination, but the root cause was NSWorkspace alone.
- Chrome for Testing and Chromium snapshot builds don't reproduce it — only official Chrome Stable with NSWorkspace launch.

## Risks
- The `openURLs` method still uses `NSWorkspace.shared.open(urls, withApplicationAt:)` for forwarding URLs to running Chrome. Tracked as passe-cinuje. Probably fine since it's not launching Chrome, just sending URLs to an existing instance.
- `open -a` returns asynchronously — the SingletonLock race might behave differently than NSWorkspace. Not observed in testing but worth watching.

## Next
- Close passe-cinuje (test URL forwarding from other apps)
- The OOPiF frame targeting outcome (passe-tikedu) has all steps done — might be ready to close

## Reflection
**Claude observed:** The investigation took several wrong turns (extensions, profile state, Chromium bisect, CfT bisect) before landing on the launch method. The key insight — that CDP connections change Chrome's behaviour — made automated testing impossible and nearly derailed the bisect. The actual fix was one function change in handler.swift.
**User noted:** Enjoyed the bisect process despite it not finding the answer. Pragmatic about not filing upstream — easy fix locally, not worth the Chromium bug tracker overhead.
