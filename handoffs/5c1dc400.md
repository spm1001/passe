# Handoff — 2026-03-21

session_id: 5c1dc400-1891-497b-bbe9-a97449d93f81
purpose: Shipped daemon lifecycle, tab management, closed both open outcomes

## Done
- Added daemon lifecycle CLI: start/stop/status/pause/unpause (passe-remato)
- Fixed 11 robustness issues in lifecycle module (PermissionError, file handle leak, pre-flight check, daemon.log rotation, --json on status, etc.)
- Updated CLAUDE.md with daemon docs, tested trigger phrases via ardoise naive Claude (passe-pufado)
- Closed passe-fakuba outcome: Chrome network capture fully absorbed from skill-chrome-log
- Added --keep-tab auto-replace: closes same-origin tabs before creating new one (passe-vodowu)
- Added passe tabs subcommand: list/close tabs via CDP (passe-viwive)
- Closed passe-duceri outcome: stray tab cleanup
- Fixed Chrome going windowless when passe closes the last tab (newWindow:true safety check)
- Fixed log tail/list graceful empty output after log clear
- Enriched CLAUDE.md with --file flag docs for query tools

## Gotchas
- Target.getTargets returns empty on fresh CDP connections to remote Chrome (Mac via tailscale). Only works after Target.setDiscoverTargets. The /json HTTP endpoint always works. close_tab() uses /json but the other 4 tab methods still use Target.getTargets — they silently fail on remote Chrome. This is the main open bug (passe-zutoda).
- The autouse fixture in test_tab_lifecycle.py mocks urllib.request.urlopen globally — any new code using urlopen in client.py methods needs to account for this.
- Chrome extension URLs (like ddkjiahejlhfcafbddmgiahcphecmpfh) leak through --filter — missing pattern in SKIP_URL_PATTERNS.

## Risks
- --keep-tab auto-replace uses close_tabs_by_origin which calls Target.getTargets — so auto-replace doesn't actually work on Mac Chrome. It works on local headless Chromium. The CLAUDE.md documents it as working everywhere.
- The newWindow:true safety check in close_tab opens about:blank, not the new tab page. Functional but slightly jarring.

## Next
- passe-zutoda: Unified _get_pages() helper — the priority fix. Plan written at ~/.claude/plans/shimmering-gliding-mitten.md with full implementation detail. Affects client.py (5 methods), test_tab_lifecycle.py, and one line in log_daemon.py.
- After that, passe backlog is clear.

## Commands
```bash
# Verify current state
uv run python -m pytest tests/ --ignore=tests/bench --ignore=tests/test_log_daemon_integration.py --ignore=tests/test_read_integration.py -x
# 7 unpushed commits on main
git log --oneline origin/main..HEAD
```

## Reflection
**Claude observed:** The Target.getTargets vs /json discrepancy is a cross-cutting bug we only partially fixed. We patched close_tab (the symptom we discovered) but left 4 other call sites broken. The ardoise testing of CLAUDE.md was valuable — confirmed trigger phrases route correctly, but also revealed the --file flag gap. The session was productive but the last-tab fix went through 3 iterations (chrome://newtab, newWindow, then /json endpoint) when understanding the CDP/HTTP gap first would have led to a single clean fix.
**User noted:** The pufado action was stale (3 of 6 steps already done) — should have checked preconditions before drawing down. The close_tab testing required manual timing coordination between user and Claude to reproduce the windowless Chrome case.
